schema_version: 1

context:
  name: boost_mp11
  version: "1.91.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://github.com/boostorg/mp11/archive/refs/tags/boost-${{ version }}.tar.gz
  sha256: 6ab871e0ef397a2e7b0602f13a5f473a7381043c91b57fe907ca4925a6a1ee58

build:
  number: 0

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ stdlib("c") }}
    - ${{ compiler('cxx') }}
    - cmake
    - ninja
  run_constraints:
    - boost-cpp <0a0

tests:
  - script:
      - if: unix
        then: test -f $PREFIX/include/boost/mp11.hpp
      - if: win
        then: if not exist %LIBRARY_INC%\\boost\\mp11.hpp exit 1

about:
  summary: C++11 metaprogramming library
  license: BSL-1.0
  license_file: LICENSE_1_0.txt
  homepage: https://github.com/boostorg/mp11

extra:
  recipe-maintainers:
    - xhochy
